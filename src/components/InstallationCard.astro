---
const { data } = Astro.props;
const { navigation } = Astro.props;
console.log(data);
---

<article>
  <a href="">
    <div class="nr1">
      <img class="card-bg" src={data.image} alt={data.name} />
      <div class="content-overlay">
        <h4>{data.name}</h4>
        <p>{data.date}</p>
      </div>
      <div class="card_bund">
        <a href={navigation}>
          Learn more <i class="fa-solid fa-chevron-right fa-0.5x" style="color: #ffffff;"></i>
        </a>
      </div>
    </div>
  </a>
</article>

<style>
  article {
    border: solid white 1px;
  }

  .nr1 {
    position: relative;
  }

  .card-bg {
    width: 100%;
    aspect-ratio: 16/8;
    object-fit: cover;
    filter: brightness(25%);
  }

  .nr1 img:hover {
    filter: none;
  }

  .content-overlay {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
    white-space: nowrap;
  }

  .card_bund a {
    position: absolute;
    bottom: 10px;
    left: 20px;
    display: inline-block;
    font-size: 1rem;
    padding: 12px 0;
    color: var(--text-color);
    text-decoration: none;
    cursor: pointer;
    transition: all 0.55s ease;
  }
  .card_bund a:hover {
    transform: scale(1.1);
  }
</style>
